<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ExactAlarmPolicy" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> <!-- Android 13+ 向け（ランタイムで要求する必要あり） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <attribution
        android:label="@string/smc_music_label"
        android:tag="sense_music_player" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SenseMusicClock" >
        <receiver
            android:name=".ui.Widget.ControlWidgetReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ui.ControlWidget.clickedPlay" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ui.ControlWidget.clickedNext" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ui.ControlWidget.clickedPrev" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/control_widget_info" />
        </receiver>

        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".ui.list.ListsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.StandardPlayerActivity"
            android:exported="false"
            android:label="@string/title_activity_standard_player"
            android:theme="@style/Theme.SenseMusicClock.Fullscreen" />

        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback" >
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <service
            android:name=".Clock.AlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <service
            android:name=".Clock.TimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <service
            android:name=".Clock.StopWatchService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".Clock.AlarmReceiver"
            android:exported="false" >
            <intent-filter>
                <action android:name="jp.gr.java_conf.SenseMusicClock.ACTION_ALARM_START" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ACTION_SET_ALARM" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ACTION_STOP_ALARM" />
                <action android:name="jp.gr.java_conf.SenseMusicClock.ACTION_CANCEL_ALARM" />
            </intent-filter>
        </receiver>
    </application>

</manifest>