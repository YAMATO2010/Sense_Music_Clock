/*
package jp.gr.java_conf.SenseMusicClock.Music.forSpotify

import android.content.Context
import jp.gr.java_conf.SenseMusicClock.SpotifyTrack

interface SpotifyAdapter {
    fun connect(context: Context, onConnected: (success: Boolean, error: Throwable?) -> Unit)
    fun disconnect()
    fun isConnected(): Boolean
    fun playSpotifyUri(spotifyUri: String)
    fun pause()
    fun resume()
    fun subscribeToPlayerState(onState: (SpotifyTrack) -> Unit)
    fun unsubscribeFromPlayerState()
}

 */